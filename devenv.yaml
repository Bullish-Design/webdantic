# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  # devenv's own `integrations/git-hooks.nix` throws unless one of `git-hooks`
  # or `pre-commit-hooks` is declared, and it is reached while evaluating
  # `config.shell` — so without this the shell does not enter AT ALL:
  #
  #   error: git-hooks or pre-commit-hooks input required
  #
  # `pydantree` carries the same template and the same direct shellij import,
  # and works because it declares this. Adding it is what makes this repository
  # adoptable (STAGE_7_LOG.md, wave 2b).
  pre-commit-hooks:
    url: github:cachix/pre-commit-hooks.nix
  # shellij: durable Zellij workbenches. This repo does not import RepoMan's
  # meta-module, so it imports shellij's devenv module directly — shellij/zellij/
  # yazi packages, the guarded `shellij open` enterShell hook, and
  # YAZI_CONFIG_HOME pointing at the packaged Yazi assets.
  shellij:
    url: path:/home/andrew/Documents/Projects/shellij

  # devman: the automation plane's repo interface (CONCEPT.md §3.2). A devenv
  # import is a PATH inside an input, never a flake output attribute, and the
  # file it resolves to must be named devenv.nix.
  devman:
    url: "git+https://github.com/Bullish-Design/devman?ref=main&rev=50c4c2e841136496b05fc1bf1037c9eba0133db7"

imports:
  - shellij/modules
  - devman/modules

# If you're using non-OSS software, you can set allowUnfree to true.
# allowUnfree: true

# If you're willing to use a package that's vulnerable
# permittedInsecurePackages:
#  - "openssl-1.1.1w"

# If you have more than one devenv you can merge them
#imports:
# - ./backend
